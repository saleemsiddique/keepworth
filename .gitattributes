# The project builds on macOS and the hooks are shell scripts: with CRLF they fail to
# run. LF is forced across the whole repository, no matter where a file is edited.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
*.xcassets/** -text
